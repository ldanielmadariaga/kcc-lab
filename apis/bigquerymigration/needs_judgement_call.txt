# Fields the generator emitted mechanically but cannot vouch for.
# Each entry needs a human decision: is it a reference, should it be omitted,
# should it be renamed for KRM conventions?
#
# While a resource has entries here, TestMissingRefs suppresses its [refs]
# findings. Clearing them graduates the resource and the ratchet applies.

kind=BigQueryMigrationMigrationWorkflow group=bigquerymigration.cnrm.cloud.google.com: resource reason=untriaged-bulk-generation (spec was generated mechanically; confirm refs, omissions and KRM names)
kind=BigQueryMigrationMigrationWorkflow group=bigquerymigration.cnrm.cloud.google.com: field ".status.observedState.name" reason=observedstate-identity-field-omitted (proto marks it OUTPUT_ONLY; KCC carries the resource name in status.externalRef instead. Confirm that is right for this resource)

# Fields dropped because the generator could not produce a Go type for
# them. They are absent from the CRD. Shared nested messages, so they are
# listed per service rather than against one Kind.
# dropped: google.cloud.bigquery.migration.v2alpha.TranslationTaskDetails.specialTokenMap reason=unsupported map type with key string and value enum
